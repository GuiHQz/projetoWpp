import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
    "& *": {
        boxSizing: "border-box"
    },

    "& body": {
        backgroundColor: "#D2DBDC",
        margin: 0,
        fontFamily: "'Segoe UI', Helvetica Neue, Helvetica, Lucida Grande, Arial"
    },

})